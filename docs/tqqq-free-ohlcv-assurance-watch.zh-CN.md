# TQQQ 免费 OHLCV 数据源校准观察

`TQQQ V9 Free OHLCV Assurance Calibration` 在每个已完成的美股交易日后，以 Twelve Data 与 Yahoo 的拆股调整日线做一次只读交叉验证。它只保留脱敏状态、哈希和按 OHLC 字段的差异分布；不会上传价格、成交量、原始响应、路径或凭据。

这不是回测、P3、影子、平台操作或交易任务。即使 P1 数据源完全一致，工作流也只记录 `VERIFIED`，不会自动创建研究输入、修改 V9、放宽容差、选择数据源、切换策略或推进任何上线阶段。

当记录到足够多个独立已完成交易日后，后续校准必须单独冻结新的候选数据合约，并用该候选重新回测和审计；历史 V9 的 1bp 全 OHLC 规则始终保持不变。单次或少量差异绝不能自动改写策略或风险阈值。
